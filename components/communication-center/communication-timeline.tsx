import { Mail } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { formatDate, formatTime } from "@/lib/date";
import type { Communication } from "@/lib/types";
export function CommunicationTimeline({communications}:{communications:Communication[]}){return <div className="space-y-0">{communications.map((x,i)=><div key={x.id} className="relative flex gap-4 pb-6"><div className="relative z-10 grid h-9 w-9 shrink-0 place-items-center rounded-full bg-navy-50 text-navy-700"><Mail size={15}/></div>{i<communications.length-1&&<span className="absolute left-[17px] top-9 h-full w-px bg-slate-200"/>}<div className="min-w-0 flex-1 rounded-xl border border-border bg-white p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex gap-2"><Badge tone="blue">{x.channel}</Badge><Badge tone={x.status==="SENT"?"green":x.status==="FAILED"?"red":"amber"}>{x.status}</Badge></div><span className="text-xs text-slate-500">{formatDate(x.created_at)} · {formatTime(x.created_at)}</span></div><p className="mt-2 font-bold text-navy-900">{x.subject||"No subject"}</p><p className="mt-1 text-xs text-slate-500">{x.creator?.full_name||x.creator?.email||"Unknown user"}</p></div></div>)}{!communications.length&&<p className="py-8 text-center text-sm text-slate-500">No communications yet.</p>}</div>}
